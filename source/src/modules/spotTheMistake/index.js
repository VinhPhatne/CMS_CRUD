import React from 'react';
import { FormattedMessage } from 'react-intl';

const SpotTheMistakeModule = () => {
    return (
        <div>
            <h1>
                <FormattedMessage id="spotTheMistake.title" defaultMessage="Spot the Mistake Game" />
            </h1>
        </div>
    );
};

export default SpotTheMistakeModule;
